#site-header-menu.fr
  ul.site-header-link-menu
    li(v-repeat="item in imgs")
      a(class="img img-header-menu {{item.className}}", href="{{item.href}}")
        i.img.img-info-new(v-if="item.isNew")
